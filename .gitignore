build/
# nested host-test build dirs (e.g. components/*/test/build_verify/) —
# the bare `build/` above only matches dirs literally named "build"
components/*/test/build*/
managed_components/
dependencies.lock
sdkconfig.old
sdkconfig.ci.*
# local secret overlay (bootstrap API token etc.) — never committed
sdkconfig.local.defaults
compile_commands.json
.cache/
.vscode/
.idea/
.DS_Store
# transient code-review scratch — never committed
REVIEW_FINDINGS.md
